name: hearty_app
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.11.5

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # Audio / Voice
  flutter_tts: ^4.2.2
  shared_preferences: ^2.3.3
  speech_to_text: ^7.0.0
  just_audio: ^0.10.4
  sherpa_onnx: ^1.13.2
  path_provider: ^2.1.3
  wakelock_plus: ^1.2.8 # keep screen awake during a voice session
  # Wake word: openWakeWord via ONNX Runtime (inference runs in Kotlin foreground service,
  # not Dart — no Flutter package needed; Android dependency added in build.gradle)

  # Camera / Media
  camera: ^0.11.1
  mobile_scanner: ^6.0.5
  image_picker: ^1.1.2
  cached_network_image: ^3.4.1

  # Database / Offline
  drift: ^2.26.1
  drift_flutter: ^0.2.5

  # Networking
  dio: ^5.9.0
  connectivity_plus: ^6.1.4
  uuid: ^4.5.1

  # Supabase / Auth
  supabase_flutter: ^2.9.1
  google_sign_in: ^6.3.0

  # Firebase / Notifications
  firebase_core: ^3.13.1
  firebase_messaging: ^15.2.5
  flutter_local_notifications: ^18.0.1
  timezone: ^0.10.0
  flutter_timezone: ^3.0.0

  # Navigation
  go_router: ^14.8.1

  # Charts / UI
  fl_chart: ^0.70.2

  # State management
  flutter_riverpod: ^2.6.1
  hooks_riverpod: ^2.6.1
  flutter_hooks: ^0.21.2
  riverpod_annotation: ^2.6.1

  # Background / Permissions
  workmanager: ^0.9.0
  permission_handler: ^11.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # Code generation
  build_runner: ^2.4.15
  drift_dev: ^2.26.1
  riverpod_generator: ^2.6.3
  custom_lint: ^0.7.6
  riverpod_lint: ^2.6.5

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/wake_word/hey_hearty.onnx
    - assets/wake_word/melspectrogram.onnx
    - assets/wake_word/embedding_model.onnx
    - assets/audio/wake_chime.wav
    # TTS: Piper VITS en_US-libritts_r-medium (sherpa_onnx offline TTS)
    - assets/tts/hearty-neutral/
    - assets/tts/hearty-neutral/espeak-ng-data/
    - assets/tts/hearty-neutral/espeak-ng-data/lang/
    - assets/tts/hearty-neutral/espeak-ng-data/lang/aav/
    - assets/tts/hearty-neutral/espeak-ng-data/lang/art/
    - assets/tts/hearty-neutral/espeak-ng-data/lang/azc/
    - assets/tts/hearty-neutral/espeak-ng-data/lang/bat/
    - assets/tts/hearty-neutral/espeak-ng-data/lang/bnt/
    - assets/tts/hearty-neutral/espeak-ng-data/lang/ccs/
    - assets/tts/hearty-neutral/espeak-ng-data/lang/cel/
    - assets/tts/hearty-neutral/espeak-ng-data/lang/cus/
    - assets/tts/hearty-neutral/espeak-ng-data/lang/dra/
    - assets/tts/hearty-neutral/espeak-ng-data/lang/esx/
    - assets/tts/hearty-neutral/espeak-ng-data/lang/gmq/
    - assets/tts/hearty-neutral/espeak-ng-data/lang/gmw/
    - assets/tts/hearty-neutral/espeak-ng-data/lang/grk/
    - assets/tts/hearty-neutral/espeak-ng-data/lang/inc/
    - assets/tts/hearty-neutral/espeak-ng-data/lang/ine/
    - assets/tts/hearty-neutral/espeak-ng-data/lang/ira/
    - assets/tts/hearty-neutral/espeak-ng-data/lang/iro/
    - assets/tts/hearty-neutral/espeak-ng-data/lang/itc/
    - assets/tts/hearty-neutral/espeak-ng-data/lang/jpx/
    - assets/tts/hearty-neutral/espeak-ng-data/lang/map/
    - assets/tts/hearty-neutral/espeak-ng-data/lang/miz/
    - assets/tts/hearty-neutral/espeak-ng-data/lang/myn/
    - assets/tts/hearty-neutral/espeak-ng-data/lang/poz/
    - assets/tts/hearty-neutral/espeak-ng-data/lang/roa/
    - assets/tts/hearty-neutral/espeak-ng-data/lang/sai/
    - assets/tts/hearty-neutral/espeak-ng-data/lang/sem/
    - assets/tts/hearty-neutral/espeak-ng-data/lang/sit/
    - assets/tts/hearty-neutral/espeak-ng-data/lang/tai/
    - assets/tts/hearty-neutral/espeak-ng-data/lang/trk/
    - assets/tts/hearty-neutral/espeak-ng-data/lang/urj/
    - assets/tts/hearty-neutral/espeak-ng-data/lang/zle/
    - assets/tts/hearty-neutral/espeak-ng-data/lang/zls/
    - assets/tts/hearty-neutral/espeak-ng-data/lang/zlw/
    - assets/tts/hearty-neutral/espeak-ng-data/voices/!v/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
